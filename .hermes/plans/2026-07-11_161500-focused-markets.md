# Focused Markets Collector Plan

- [x] Reuse `buildHandicapCards` via a thin totals adapter; add one focused totals leave-one-out test.
- [x] Request `spreads,totals`, cache both, save `totals-loo-v1` snapshots, run HKJC importer every 15 minutes.
- [x] Serve both caches to Dashboard, settle totals from existing score results, verify cron/tests/build/browser.

Ponytail: no new generic framework; the totals adapter maps Over/Under onto the already-tested two-way HDC engine.
