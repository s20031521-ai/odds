# Production Phase 2 SDD Progress

- Task 0: complete - VM preconditions verified 2026-07-19. Key-only SSH login confirmed from fresh BatchMode session (existing `astra_vm_ed25519` key, account `hugo`, port 169). Password rotated to random 40-hex then changed back to the owner-preferred value same session at the owner's explicit request (compromised-value risk flagged and accepted by owner; key-only SSH is the verified primary path). Owner directive recorded: never delete/disable the `hugo` login, never disable SSH password auth or rotate the password unless explicitly asked. Inventory: astra (3 containers, healthy), store-network-dashboard (healthy), odds-tool-test (disposable test DB, healthy); Docker 29.6.1 / Compose 5.3.1; 61 GiB free disk; outbound HTTPS to Cloudflare and The Odds API confirmed. `/opt/odds-tool` created empty (hugo:hugo). Report: `.superpowers/sdd-production-phase2/task-0-report.md`. Open owner decisions: disable SSH password auth (safe now), provider-level VM snapshot before Task 1.
- Task 1: complete - Compose skeleton on VM 2026-07-19. `deploy/compose.yaml` (project odds-tool, 3 networks with internal db_net, pgdata volume, no published ports), digest-pinned postgres:18-bookworm (18.4), secrets pg_postgres/app/migration_password at 0400 root (generated on-VM). Deviation: VM Docker ignores secret uid/gid/mode (mounts root-only), so role bootstrap moved from initdb.d to idempotent `deploy/postgres/create-roles.sh` via docker exec. Migrations 001-003 applied via one-shot node container (migration role), ledger confirmed. Gate: postgres healthy, zero new host listeners, compose config clean, least-privilege proven (odds_app SELECT ok, DDL denied). Task 2 requirement recorded: api needs RUN_MIGRATIONS=false path (entry.mjs migrates at startup; odds_app has no DDL). Report: task-1-report.md.
- Task 2: complete - api/caddy images and edge on VM 2026-07-19. D2-A trusted proxy implemented with strict TDD (client-ip helper, TRUSTED_PROXY_CIDRS, RUN_MIGRATIONS=false path; config 5/5, client-ip 7/7, app proxy 3/3; DB suites re-run 49 pass/1 skip/0 fail; Vitest 149/149; archives unchanged). Digest-pinned api (node 24.18.0) + caddy (2.11.4 with PWA dist) built on VM. Incidents fixed: (1) Docker root-only secrets forced root-start entrypoint + setpriv drop to uid 1000; (2) Caddy try_files directive-order swallowed /api/v1/* and /internal/* — rewrote with ordered handle blocks. Private-network probes all PASS: PWA 200, session JSON, /internal/* 404 at caddy vs 200 direct, full security headers. Zero host ports; images secret-free. Report: task-2-report.md.

- Task 3: complete - Archive bundle migration into production PostgreSQL 2026-07-19. 9 archives hashed locally, scp'd to dated read-only VM bundle `/opt/odds-tool/migration-bundle-2026-07-19/`, re-hashed 9/9 byte-identical (note: 2 background jsonl files are legitimately 0-byte). One-shot api-image containers on db_net with migration role: import #1 inserted 96 snapshots / 87 rejected-to-audit / 853 results; import #2 fully idempotent (all already-complete, zero additions); parity status=ok with exact Phase 1 numbers (183/853, 3 valid-current / 93 legacy / 87 invalid, 286 distinct matches, 0 settlements); integrity --database exit 0 with all failure counters 0. Post-migration hashes byte-identical both sides. `deploy/migration-bundle/.gitkeep` documents the no-VCS bundle procedure. Report: task-3-report.md.
- Task 4: complete - Owner bootstrap 2026-07-19. Username s20031521; password (owner-supplied, 14 chars, policy-compliant) staged via SSH stdin as 0400 root secret file, create-owner status=created, exactly one owners row verified. Private login matrix through caddy all PASS: wrong pw generic 401; correct 200 + __Host-odds_session (Secure/HttpOnly/SameSite=Strict); session authenticated; logout without CSRF 403; with CSRF 200 and revoked (post-logout session authenticated:false). Cleanup: 2 probe sessions deleted, login_attempts 0, password file deleted from secrets dir, temp files removed both sides. Report: task-4-report.md.
- Task 5: complete - Private-network smoke tests 2026-07-19. Pre-step deviation: Caddyfile tightened (handle /api/*, /hkjc-odds.json, /health -> 404) so legacy paths fail closed at the edge matching Phase 1 parity; caddy rebuilt on VM. Probe matrix 29 PASS / 0 FAIL / 0 error-body leaks (task5-probe.mjs): unauth 401 x4, identical 401s, malformed 400, 16KiB/1MiB 413, cookie flags exact, Origin/CSRF 403 x3, 404/405, /internal 404, legacy 404 x6, authed data (853 results, backtest parity-consistent, odds/live valid), logout revoke, throttle 429 on 5th. Collector posture: no collector service in compose (paid disabled by construction), odds_api_key installed but mounted nowhere, all 3 self-tests pass under --network none; optional hkjc-import one-shot deferred pending owner approval. PWA: shell+sw+manifest 200; runtimeCaching empty, API denylisted from SW. Cleanup done (sessions/login_attempts wiped, owner pw file re-deleted). Report: task-5-report.md.
- Task 6: mostly complete - Cloudflare Tunnel live 2026-07-19. Owner-approved one-shot hkjc-import first exposed a real least-privilege gap (odds_app missing sequence USAGE); create-roles.sh fixed (sequences default+explicit grants), synced both VM locations, re-run converged; second import wrote 37/37/28/30 live odds + 717 result comparisons (live_odds=301, results=1234, collector_state=1). cloudflared digest-pinned in compose, token via root-only env_file, 4/4 QUIC connections to HKG edge. Incident: dashboard public hostname first pointed at caddy:88 (refused); owner fixed to caddy:80. Public matrix all PASS (PWA 200, 401s, /internal+legacy 404, headers, full login/logout flow; one probe sed miss briefly printed a session token — revoked + row deleted immediately). Exposure audit: zero new host listeners. Step 4 (enable paid collector) pending explicit owner approval. Report: task-6-report.md.
  Task 6 Step 4: complete - paid collector enabled 2026-07-19 (explicit owner approval "開"). collector-entrypoint.sh (root->secrets->setpriv uid 1000->supervisor loop: hdc-collector every 5 min state-driven zero-call-when-idle + hkjc-import every 3rd cycle); collector service on api image with 3 secrets, STORAGE_BACKEND=postgres. Incident: hkjc-import EACCES mkdir /app/public as uid 1000 -> Dockerfile mkdir+chown fix, rebuilt. Verified: hkjc-import 37/37/28/30 + 717 comparisons; hdc-collector discovered real EPL fixtures, quotaRemaining=500 (healthy); snapshots=96/live_odds=301/results=1234. Redundant cloudflared_token file removed; secrets README corrected to env_file mechanism.
- Task 7: complete - Runbook + rehearsed rollback 2026-07-19. docs/runbooks/production-deployment.md written (deploy order, readiness, logs, rollback incl. tunnel kill-switch + pg_dump restore points, secret rotation table, failure playbook). Rollback rehearsed: tagged A (057fce31) as rollback, deployed marked image B (4305d48e), rolled back to A (verified marker gone + readiness ok), rolled forward to B, rebuilt clean, rollback tag re-pointed, rehearsal tag pruned. Rehearsal caught a real operator error (compose up without cd /opt/odds-tool -> silently stayed on B) — recorded. Post-rehearsal public smoke 200/200/401/404; all containers healthy. PHASE 2 COMPLETE. Report: task-7-report.md.
- Post-Phase-2 incident (2026-07-19, found on live watch): collector loop crashing every cycle. TWO production bugs fixed with TDD: (1) hdc-collector loads parsers via Vite ssrLoadModule("/src/oddsApi.ts") but api image did not ship src/ -> Dockerfile now COPYs src/; (2) liveOddsIdentity lacked bookmaker, so multi-bookmaker Odds-API batches collided on live_odds_identity_key_key (23505) -> identity gains |<bookmaker> suffix when present (legacy 5-part form preserved when absent). RED reproduced exactly in disposable DB (same 23505), then GREEN: domain 20/20, repositories 12/12 (new multi-bookmaker test), full suites 49/49 0 fail. Deployed: live_odds now hkjc=292 + the-odds-api:soccer_mexico_ligamx=32; hdc cycle healthy 22:58; quotaRemaining 500->491 (Liga MX fetch); snapshots=96 (off-season, expected). Rollback tag re-pointed at fixed image. Files synced local<->VM: identity.mjs, backtest.test.mjs, repositories.test.mjs, api.Dockerfile.
- Feature (2026-07-19, owner request): league name on every dashboard card. Implemented via coder subagent with TDD (Vitest 149->155, self-tests green, pg tests 17/17, build ok): HKJC parsers emit league from tournament.name_en/name_ch; hdc-collector attaches sport_title via withLeague(); flatten functions + frontend thread league Fixture->BuyCandidate->BuyDashboard/AllFixtures cards (conditional render). Deployed: both images rebuilt on VM; live_odds hkjc 270/270 rows now carry real league names (Australian NPL, USL, Korean Div 1/2, WC Finals etc.); public payload has 540 league fields; built bundle contains dashboard-card__league. the-odds-api rows gain league at next natural refresh (state-driven quota saving). Rollback tags re-pointed.
- Incident (2026-07-19): league UI invisible to owner after deploy. Root cause: Cloudflare edge cached old sw.js (.js cached by default; origin had no Cache-Control) — browsers kept installing the pre-league app bundle via SW precache even though index.html/origin were new. Fix: Caddyfile now sends Cache-Control "no-cache, must-revalidate" for /sw.js + /registerSW.js; caddy rebuilt; owner purged Cloudflare cache (Purge Everything). Verified: public sw.js cf-cache-status MISS, precaches new bundle index-BP0Hv_WA.js; owner confirms league names visible. Lesson recorded: any future static-asset caching issues -> check cf-cache-status first; consider Cloudflare cache rules for HTML/SW if Browser Cache TTL is ever changed.
- Feature (2026-07-19, owner request): Chinese team names in UI, English canonical in data layer. hkjc-import parsers emit homeTeamZh/awayTeamZh from name_ch (homeTeam/awayTeam stay EN for matching/identity); frontend threads Zh fields same path as league, render-time resolution zh ?? en at all card types (BuyDashboard + 3 fixtures-page cards). TDD by resumed coder subagent: Vitest 155->161, self-tests green, pg tests 18/18, build ok. Deployed: both images rebuilt; live_odds hkjc 177/177 rows carry Zh names (奧基迪, 哈馬比, 奧林匹克FC etc.); public payload 177/177 with EN preserved alongside; bundle index-B8B-93PR.js has homeTeamZh; SW no-cache from earlier fix means immediate rollout. The Odds API entries have no Zh -> render English fallback as designed.
- Feature (2026-07-19, owner request): Chinese league names. leagueZh added alongside EN league (HKJC tournament name_ch; The Odds API stays EN-only by design); frontend renders leagueZh ?? league at all 4 card types. TDD by resumed coder subagent: Vitest 161->167, self-tests green, build ok; DB tunnel had died — reopened and full DB suites re-run locally 47/47 0 fail. Deployed: both images rebuilt; live_odds hkjc 168/168 rows carry leagueZh (瑞典超級聯賽, 南韓職業聯賽, 世盃 etc.); public payload 168/168; bundle index-p-6jO2dh.js live.
